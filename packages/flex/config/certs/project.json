{
  "prefix": "@flexiness",
  "sourceRoot": "packages/flex/config/certs/src",
  "projectType": "library",
  "targets": {
    "build:lib": {
      "executor": "nx:run-script",
      "inputs": [
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.esm.build.json",
        "{projectRoot}/tsconfig.cjs.build.json",
        "{projectRoot}/src"
      ],
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "script": "build:lib"
      }
    }
  },
  "implicitDependencies": [],
  "tags": ["scope:flexiness-staging"]
}
