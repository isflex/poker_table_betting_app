{
  "prefix": "@flexiness",
  "sourceRoot": "packages/flex/config/eslint/src",
  "projectType": "library",
  "targets": {
    "build:lib": {
      "executor": "nx:run-script",
      "inputs": [
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.build.json"
      ],
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "script": "build:lib"
      }
    }
  },
  "implicitDependencies": [],
  "tags": ["scope:flexiness-staging"]
}
