---
title: Formulaire
active: {
    components: {
        form: true
    }
}
root: ../
---
{{#extend "menu-page"}}
    {{#content "content"}}
        {{#> components-title desc="Le formulaire est employé dans un parcours, il est nécessaire de récolter des informations concernant l'utilisateur. Il permet alors de saisir du texte, cocher des cases, effectuer un choix dans une liste, appuyer sur des boutons, etc. Les éléments du formulaire doit être contenu dans une balise <form></form>"}}
        {{/components-title }}
        <section class="section">
            <div class="container">
                <div class="columns">
                    <div class="column">
                        <div class="rows">
                            <div class="row">
                                <p class="title is-5">Le composant formulaire a été scindé en plusieurs composants, voir ci-dessous :</p>
                            </div>
                            <div class="row">
                                <p><a href="{{ root }}components/checkradio.hbs" class="link">Checkradio</a></p>
                            </div>
                            <div class="row">
                                <p><a href="{{ root }}components/entry.hbs" class="link">Saisie</a></p>
                            </div>
                            <div class="row">
                                <p><a href="{{ root }}components/select.hbs" class="link">Select</a></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    {{/content}}
{{/extend}}
