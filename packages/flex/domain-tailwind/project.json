{
  "prefix": "@flexiness",
  "sourceRoot": "packages/flex/domain-tailwind/src",
  "projectType": "library",
  "targets": {
    "build:lib": {
      "executor": "nx:run-commands",
      "inputs": [
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.build.json"
      ],
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "yarn workspace @flexiness/domain-tailwind 'build:lib'"
      }
    }
  },
  "implicitDependencies": [],
  "tags": ["scope:flexiness-staging"]
}
